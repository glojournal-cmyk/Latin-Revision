# Latin Revision V6 — 更新及溫習方法

## 今次版本有甚麼

- 847 條 source-locked 題目，只限 18 July–3 September 2026 Summer Revision。
- 每個日期有獨立題池；較後內容不會混入較早日期。
- 23 August 才開始 `ad urbem`；24–25 August 才開始 `ad villam`；28 August 才有 Ancient Greece。
- 每次先看 2–4 分鐘 Quick Notes，再隨機抽 12–15 題。
- 自動批改；set-text 翻譯接受多個合理英文寫法，毋須學生自行判分。
- 85% mastery。
- 錯題 2 日後再出；答對後 7 日後再出；第二次答對才清除。

## 用 Windows 更新現有 GitHub Pages

1. 下載新版 ZIP，右鍵選 **Extract All / 解壓縮**。
2. 打開解壓後的 `Latin_Revision_PWA_V6_Summer_SourceLocked` folder。
3. 在 Windows 瀏覽器登入 GitHub，進入你現有的 `latin-revision` repository。
4. 按 **Add file → Upload files**。
5. 把 folder **裡面的所有檔案和 `icons` folder** 拖到 GitHub；不要把最外層 folder 當成一個新子目錄。
6. 必須見到以下項目：
   - `index.html`
   - `question-bank.js`
   - `manifest.webmanifest`
   - `sw.js`
   - `icons/icon-192.png`
   - `icons/icon-512.png`
7. 頁面底部按 **Commit changes**。
8. 不用再改 Settings → Pages。等待約 1–3 分鐘，GitHub Pages 會自動重新發佈。

## 在 iPad 收到新版

1. 保持 iPad 有 Wi-Fi。
2. 完全關閉主畫面的 **Latin Revision** app：由畫面底部向上掃並停頓，再把 app 預覽向上推走。
3. 用 Safari 打開原本 GitHub Pages 網址，按一次重新整理。
4. 關閉 Safari，再由 Home Screen 打開 **Latin Revision**。
5. 如仍是舊版，再完全關閉 app 並重開一次。
6. 頁面頂部應顯示 **Summer V6**，首頁題庫數目應顯示 **847**。

不用刪除 Home Screen icon，也不用重新 Add to Home Screen。舊分數紀錄會盡量保留；由於原本 23 題已換成全新編號題庫，舊 weak-question list 會重新開始。

## 女兒每天應怎樣用

1. 先按首頁的 **Due today**。如有到期錯題，先完成它們。
2. 再按正在溫習的日期；不要直接跳到 30 August full assessment。
3. 看 Quick Notes 2–4 分鐘，之後關閉筆記開始做題。
4. 每次只做 app 隨機抽出的 12–15 題，不需要一次做完整題池。
5. 分數未達 85%：看清楚 feedback，同一天稍後或翌日再做同一日期。
6. 達 85%：轉下一個日期，但仍要完成 2-day / 7-day 到期重溫。
7. 完成前面日期後，才用 **Mixed test from attempted blocks**。
8. 30 August 做 30 題 full-coverage mini-assessment；31 August 及 1 September 只做錯題／到期重測，不學新內容。

建議平日每次 10–20 分鐘。完整長卷保留作每 1–2 星期的驗收；日常主要用 app 做針對性溫習。

## 更新前備份

在舊版或新版首頁按 **Export progress**，把 JSON file 留在 Files。新版亦有 **Import progress**。

不要隨便清除 Safari 的 Website Data，否則裝置內的分數及錯題紀錄可能被刪除。
